{
  "buildCommand": "cd apps/default && npx expo export --platform web",
  "outputDirectory": "apps/default/dist",
  "installCommand": "bun install --no-optional",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://cheery-elk-110.convex.site/api/:path*" }
  ]
}
